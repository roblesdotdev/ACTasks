package com.roblesdotdev.act.login.domain.model

/**
 * A custom implementation of [Throwable] that occurs when the user tries to login
 * with invalid credentials.
 */
class InvalidCredentialsException : Throwable()
